name: workflow_02
on: [repository_dispatch, workflow_dispatch]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello :-)"
      - name: Get code
        uses: actions/checkout@v4
      - run: |
          sed -i '' -e "s|image: mhussain78/hello-flux:.*|image: mhussain78/hello-flux:0.0.2|" ./kubernetes/fluxcd/repositories/hello-flux/deployment.yaml